### abs()


In Python, the built-in function abs() returns the absolute value of a number — meaning its distance from zero on the number line.

For real numbers: removes the negative sign if present

```py
abs(5)      # 5
abs(-5)     # 5
abs(0)      # 0
abs(-3.14)  # 3.14
```